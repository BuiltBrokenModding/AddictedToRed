package com.builtbroken.addictedtored.content.voice;

/** Streams radio to the client, doesn't actually play on the server but only syncs up the clients around it.
 * Should come with a configurable range, volume server side, volume client side, and change selector.
 * Created by robert on 3/20/2015.
 */
public class TileRadioStream
{
}
